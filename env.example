# Lokal utveckling: kopiera till .env.local och fyll i värdena.
#   cp env.example .env.local
#
# Hämta värdena från: Vercel → Storage → Redis-databasen → .env.local
# Eller: vercel env pull .env.local

KV_REST_API_URL=https://xxxxx.upstash.io
KV_REST_API_TOKEN=AXxxxx...
